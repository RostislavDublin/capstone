# Deployment Configuration for Vertex AI Agent Engine

# GOOGLE_CLOUD_PROJECT - Your GCP project ID
GOOGLE_CLOUD_PROJECT="your-gcp-project-id"

# DEPLOYMENT_REGION - Agent Engine deployment region
# Available: us-central1, us-east4, us-west1, europe-west1, europe-west4
# See: https://cloud.google.com/vertex-ai/generative-ai/docs/agent-engine/overview#locations
DEPLOYMENT_REGION="us-central1"

# DEPLOYMENT_AUTH_MODE - Authentication method
# - 'service_account': Uses service-account-key.json (must exist in deployment/)
# - 'gcloud': Uses gcloud CLI (run: gcloud auth login)
DEPLOYMENT_AUTH_MODE="service_account"
